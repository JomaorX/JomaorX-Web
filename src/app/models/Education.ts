export interface Education {
  title: string;
  subtitle: string;
  institution: string;
  location: string;
  period: string;
  image: string;
  tags: string[];
  icon?: string; // opcional, como play_lesson o school
}
